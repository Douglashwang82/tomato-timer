import React from 'react'

type Props = {}

const NavBar = (props: Props) => {
  return (
    <nav className='navbar'>
        <label>Timer</label>
        <label>Works</label>
    </nav>
  )
}

export default NavBar